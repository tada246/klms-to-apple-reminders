name: klms-to-apple-reminders
options:
  bundleIdPrefix: jp.keio
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15"

settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: jp.keio.klms-to-apple-reminders
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""

targets:
  klms-to-apple-reminders:
    type: application
    platform: macOS
    sources:
      - path: klms-to-apple-reminders
    settings:
      base:
        INFOPLIST_FILE: klms-to-apple-reminders/Info.plist
        CODE_SIGN_ENTITLEMENTS: klms-to-apple-reminders/klms-to-apple-reminders.entitlements
    dependencies: []
